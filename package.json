{
  "name": "qa-engineer-scaffold",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress run --config-file=./cypress-config.json",
    "open": "cypress open --config-file=./cypress-config.json"
  },
  "author": "The Coda Collection",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "cypress": "^9.4.1",
    "mochawesome": "^7.0.1"
  }
}
